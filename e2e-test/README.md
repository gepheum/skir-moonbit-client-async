# moonbit-client-async e2e-test

This folder exercises `Service` and `ServiceClient` end-to-end against generated code.

## Setup

```bash
npx skir init
./generate.sh
```

## Run

```bash
./start-service.sh
# in another terminal
./call-service.sh
```
